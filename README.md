# Calling_Agent

1. Create an agent
2. Add a webhook to the agent
3. Set up a Google Sheet containing a row of your own phone number.
4. Loop through the rows and trigger agent phone calls using the API
5. Monitor call status
6. Push the results back into the same row of the Google Sheet once the call is marked "completed".

